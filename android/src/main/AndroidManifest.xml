<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 📱 Permissions nécessaires -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application>

        <!-- 🧩 Receiver pour redémarrage ou fermeture -->
        <receiver
            android:name="com.trendcatchadvertising.digitaldisplay.AppRestartReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.ACTION_SHUTDOWN" />
                <action android:name="android.intent.action.ACTION_CLOSE_SYSTEM_DIALOGS" />
            </intent-filter>
        </receiver>

        <!-- 🚀 Auto-démarrage après reboot -->
        <receiver
            android:name="com.trendcatchadvertising.digitaldisplay.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- ⚙️ Service foreground (doit être exporté pour être lancé depuis receiver système) -->
        <service
            android:name="com.trendcatchadvertising.digitaldisplay.AutoStartService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="dataSync|connectedDevice" />

    </application>

</manifest>
